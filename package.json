{
  "name": "yay-machine-monorepo",
  "version": "0.0.0",
  "description": "A modern, simple, lightweight, zero-dependency, TypeScript state-machine library",
  "type": "module",
  "private": true,
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "prepare": "husky",
    "all": "bun run fix && bun run check && bun run test && bun run build",
    "check": "bun run check:types && bun run check:lint && bun run check:format",
    "check:types": "turbo check:types",
    "check:lint": "npx oxlint",
    "check:format": "npx oxfmt --check",
    "fix": "bun run fix:lint && bun run fix:format",
    "fix:lint": "npx oxlint --fix",
    "fix:format": "npx oxfmt --write",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:ci": "turbo run test:ci",
    "bench": "turbo bench",
    "build": "turbo build --concurrency=1",
    "docs:sync": "bun run scripts/sync-docs.ts",
    "site:dev": "npm run -w @yay-machine/site dev",
    "site:build": "npm run -w @yay-machine/site build",
    "site:preview": "npm run -w @yay-machine/site preview"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/maurice/yay-machine.git"
  },
  "keywords": [
    "state-machine",
    "fsm",
    "state-management",
    "lightweight",
    "zero-dependency",
    "typescript",
    "browser",
    "server",
    "concise",
    "declarative",
    "deterministic",
    "safe",
    "yay"
  ],
  "author": "maurice@mauricenicholson.com",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/maurice/yay-machine/issues"
  },
  "homepage": "https://github.com/maurice/yay-machine#readme",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "@sinonjs/fake-timers": "^15.0.0",
    "@types/bun": "^1.3.13",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.2",
    "@types/sinonjs__fake-timers": "^15.0.1",
    "bun": "^1.3.13",
    "husky": "^9.1.7",
    "oxfmt": "^0.47.0",
    "oxlint": "^1.59.0",
    "playwright": "^1.53.1",
    "react": "^19.2.5",
    "react-dom": "^19.2.0",
    "sharp": "^0.34.5",
    "turbo": "^2.9.6",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": "24.x"
  },
  "packageManager": "npm@11.13.0",
  "trustedDependencies": [
    "@biomejs/biome"
  ]
}
