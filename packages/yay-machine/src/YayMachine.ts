import { MDC } from "./MachineDefinitionConfig";

export const YayMachine = {
  /**
   * Namespace for internal/advanced APIs
   */
  subtle: {
    /**
     * Machine definition config property key
     */
    MDC,
  },
} as const;
