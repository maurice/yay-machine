---
title: Counter (1, 2, 3, ...)
description: A counter state-machine that can be incremented and decremented, with configurable min/max range, demonstrating state data and conditional transitions
sidebar:
  order: 2
---

🏷️ `state data`\
🏷️ `specific state + event transition`\
🏷️ `conditional transitions`

## About

A counter that can be incremented and decremented, with configurable min/max range, demonstrating state data and conditional transitions.

## Try it

import { Aside, Code } from "@astrojs/starlight/components";

<Aside type="note" title='Did you see that?'>
The UI `<button/>` and `<input/>` events drive the machine, and the machine's state drives the UI.

Notice how the state-machine can be resumed from the previous state after refreshing the page.

Also notice how it's impossible to go below "Min" (when set) and above "Max" (when set).

</Aside>

import Counter from "./Counter.astro";

<Counter />

## Definition

Notice how the `INC` and `DEC` events are handled in **any state**, not a specific state.

{<Aside type="tip">
View this and other example's <a href="https://github.com/maurice/yay-machine/blob/main/packages/example-machines/src/counter/counterMachine.ts" target="_blank">source</a> and <a href="https://github.com/maurice/yay-machine/blob/main/packages/example-machines/src/counter/__tests__/counterMachine.test.ts" target="_blank">test</a> on GitHub

</Aside>}

import counterMachine from "@yay-machine/example-machines/src/counter/counterMachine.ts?raw";
import counterMachineUsage from "@yay-machine/example-machines/src/counter/counterMachineUsage.ts?raw";

<Code code={counterMachine} lang="typescript" title="counterMachine.ts" />

## Usage

<Code code={counterMachineUsage} lang="typescript" />
